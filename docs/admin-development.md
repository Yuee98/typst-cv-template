# Admin development ledger

Main feature branch: `codex/admin-control-plane`, based on `main@2783057292cef4ba3889d6bded31ce7863b2270f`. Accepted plan: [implementation plan](admin-control-plane-implementation-plan.md), reviewed SHA `ecba8af39b47f5c84c9486684ed4c106bc4b5f0ab74df76cf5a297a327bcba4f`.

The user authorized implementation, asynchronous development checkpoints in Relay session `6a994932-0c0c-83e8-ba9b-878517117977`, and one economical subagent when useful. Development checkpoints do not consume the final convergence budget. Continue independent implementation while a checkpoint runs; reconcile applicable findings into the same ledger. Final artifact convergence remains a separate, bounded review. Merge into main remains user-owned.

Keep a linear feature history. Workers own disjoint files in the shared checkout; the main agent creates focused commits. If a separate development branch is needed, rebase it onto the feature branch and fast-forward integrate; avoid merge commits. Never rewrite another user's branch or include unrelated `.claude/` / `.pnpm-store/` files.

| Package | Status | Evidence |
| --- | --- | --- |
| I01 contracts | in progress | successor contracts, API DTOs and portable vectors |
| I02 read-only foundation | implemented; CP1 corrections verified locally | 7 real Auth/DB tests; server/static route builds; static artifact scan; browser referrer/pageview isolation |
| I03 Provider binding schema | implemented, dark | additive v2 schema applied locally; 15 focused DB tests; v1 rows and authority root preserved |
| I04 dual runtime | implemented, dark | versioned snapshot/start lifecycle and provenance ledger; v2 resolver rejects every target pending I06 deployment reports |
| I05 runtime/legal evidence | implemented, dark | compiled capability and target bindings, sealed bilingual display, versioned availability, exact consent and request admission are implemented; production execution remains closed |
| I06 write kernel and trusted reports | implemented locally, business writes dark | real Auth TOTP/AAL2, committed-operation kernel, owner-reviewed deployment import, service-only reports, v3 execution admission and authenticated validation API |
| I07–I10 | pending | dependent on the verified I06 authority and report contracts |
| I11 optional probe | deferred release | separate full lifecycle gate |
| I12 environment release | pending | hosted identity/authorization reviewed before mutation |

No hosted migration, real Provider call, secret rotation, deployment promotion or main merge has been performed by this implementation run.

## Checkpoints

| Checkpoint | Artifact | Relay task | Findings |
| --- | --- | --- | --- |
| CP1 | PR #37, `111725b2a7647ef3c06cec0c0f6341aadc29a4c7`; I02 read-only boundary and contract drafts | `d2fe3afb-8c35-4159-b071-a2963a4c2ed9` | terminal: 2 P1 + 2 P2 applicable; corrected by `706145b` and `04ae88e`; corrected-head checkpoint pending |
| CP2 | PR #37, `c3e5e25835b6114d588c1ea718ee4c7bfe94f0c2`; corrected I02 and dark I03/I04 | `2a8f7e10-ca8d-4293-945b-b59346ebdaa4` | terminal: 1 P1 + 1 P2 applicable; same-object prepared provenance and DB-workflow paths corrected after `9cbaf1e` |
| CP3 | PR #37, `1be180b4e192fd6e49508bc64c76a60fdc17ac97`; CP2 corrections and dark I05 foundation | `a6f611b4-a3ca-47cd-9df7-80653bcb6c54` | terminal: 2 P1 + 3 P2; provider capability opacity, successor legal parsing, evidence bounds, lossless display rules and DB-workflow consumers corrected during I05 continuation |
| CP4 | PR #37, `a23cbf369b70c01be14a879577242aa17328a606`; I06 development checkpoint | `5107c44a-2975-4293-9716-35bc46c2561a` | Relay did not observe the submission (`submission_not_observed`); optional checkpoint did not block local implementation |

Local foundation verification (2026-09-04): existing local DB advanced from `20260824009000` by `migration up --local`, without reset. Full unit suite: 1,758 passed, one existing skipped. Seven focused DB tests cover bootstrap/grants, metadata forgery, bearer forwarding, projection/pagination, revoke/ban/environment/session invalidation and audit/delete invariants. Server and static builds passed; the static scan inspected 135 files. An actual dynamic `/zh/admin/profiles/<uuid>` route displayed the login page with no browser console error. Subsequent UI integration fixes (search submission, environment badges, responsive nav) are validated again before the checkpoint commit. No hosted operation or Provider transmission occurred.

The first exact-head CI run found two bounded foundation defects. CodeQL rejected a mobile navigation value that was cast from a DOM string before URL construction; navigation now resolves every section through a literal route map and rejects unknown values. The real-DB fresh-reset suite correctly detected seven new SQL routines outside its frozen authority inventory. The successor inventory now binds each approved routine by schema, name, identity arguments, kind, canonical definition hash and explicit role ACL, while the original 375-routine authority root remains unchanged. Focused CodeQL-path unit coverage and the corrected authority assertion pass locally.

Relay CP1 confirmed the authority failure and found three neighboring I02 gaps. The successor receipt now also freezes the exact `postgres` owner. Server-mode Admin responses and generated Admin documents set `Referrer-Policy: no-referrer`; both Admin-to-public links add `noreferrer`. A Playwright round trip from an Admin URL containing an email, cursor and fragment proves that the public document request, `document.referrer` and queued Vercel pageview omit the private state. GitHub OAuth returns only to the fixed locale Admin root, session restoration failures are visible, and the required DB workflow filter includes Admin server/contracts/fixture consumers. The terminal reply is preserved locally in ignored `tmp/admin-cp1-reply.md`.

Relay CP2 accepted the I02 corrections and I03 foundation, then found two I04 integration gaps. The lifecycle no longer accepts independently supplied runtime provenance: an opaque prepared execution derives the provider, runtime build ID and binding-manifest revision from the same branded transport before DB attempt admission. Crossed or copied prepared objects fail before `startAttempt` or fetch. The real-DB workflow path filter also covers every new profile/snapshot/binding/prepared-adapter consumer and its v2 fixture.

I03/I04 local progress (2026-09-04): migration `20260904000000_ai_provider_binding_v2_expand.sql` adds an adapter catalog, stable Provider identities and a strict `profile_execution_config_v2` branch without activating a v2 route or rewriting an existing execution row. Endpoint URL, `AI_PROVIDER_KEY_*` variable name and model ID are frozen on each successor profile version. The runtime validates the catalog-selected adapter against compiled support, exact official destination policy, a deployment binding manifest and a filtered secret namespace before constructing a branded transport. Synthetic transport tests prove long configured model IDs reach both request formats and retain existing usage/cost normalization. Existing v1 adapter and contract tests continue to pass. No v2 route is active, and the production v2 target resolver remains closed while the successor evidence and report chain is built.

I05 local progress (2026-09-04): migration `20260904002000_runtime_legal_evidence_v2.sql` adds the exact code-capability intersection, immutable profile/price/provider/runtime/legal target bindings, a sealed lossless text-only bilingual disclosure schema, and a protected v2 current legal identity initialized from the unchanged v1 value. Migration `20260904003000_ai_legal_acceptance_v2.sql` adds service-only disclosure/availability reads, authenticated exact-display consent and a request-ledger guard that binds every v2 request to the accepted bundle, display key and content digest. The public availability contract keeps v1 code-owned annexes compatible and carries sealed v2 display content without a model-specific frontend switch. A local configurable model fixture completes availability, exact consent, reservation, snapshot, attempt and settlement without Provider traffic. Production v2 execution remains disabled until I06 produces and verifies deployment reports.

Relay CP3 found that the first prepared execution token still exposed a mutable provider, v2 snapshot parsing still consulted the frozen v1 legal allowlist, the TS evidence bound was narrower than DB, display parsing trimmed hashed text, and two I05 consumers were absent from the DB workflow filter. The token now contains no send capability, its stored provider is immutable and private, v2 relies on exact runtime/legal evidence, both evidence bounds are 64, DB and TS share lossless whitespace/length/UTF-8 rules, and all runtime capability/legal consumers trigger the real-DB workflow. Successor-bundle, 32/33/64/65 evidence, method mutation, DB-to-DTO text round-trip and CI path tests cover the corrections.

I06 local progress (2026-09-04): migration `20260904004000_admin_write_kernel.sql` adds the authenticated Admin write authority check, live-session recent TOTP verification and durable committed-operation/idempotency kernel without enabling a business mutation. Migration `20260904005000_admin_validation_reports.sql` adds owner-reviewed deployment/capability registrations and immutable service-produced validation reports. The owner importer canonicalizes capability/evidence sets and UTC time before hashing; application roles have no importer or table DML. The authenticated Admin API accepts only registered candidate IDs, while the service producer loads the DB candidate and checks the running build, canonical binding manifest, exact official endpoint, filtered credential namespace and compiled capability without returning or recording key material.

The application now requests `get_ai_polish_execution_snapshot_v3`; every v2 execution must carry a passed, unexpired report whose reviewed deployment and current Admin environment remain valid. Snapshot parsing and the final runtime resolver compare the report to the frozen target, current project/build/manifest, current secret binding and endpoint origin before any Provider transmission. The legacy v1 execution path and the v2 database snapshot RPC remain available for in-flight compatibility. A reviewed-deployment CLI validates a strict JSON artifact, prints reviewable SQL by default, and executes through explicit DB-owner `PG*` settings only after an exact environment/project acknowledgement.

I06 verification: the final unit suite passed 1,855 tests with one existing skip; typecheck and affected-file lint passed. A real local Auth TOTP enrollment/challenge produced AAL2/TOTP claims accepted by the DB, while stale factor evidence was rejected. The final CFG-001 fresh-reset gate passed 41 tests, and the final affected real-DB files passed 11 tests. An earlier complete serial real-DB run passed 444 tests across 40 files; after the bounded importer canonicalization change, the clean fresh gate and affected files were rerun. Server → static → server builds passed, and the static artifact scan inspected 135 files with no Admin/AI server artifact.

Migration `20260904001000_provider_execution_v2_lifecycle.sql` adds service-role-only versioned snapshot/start entry points without modifying the v1 routines. A v2 attempt freezes endpoint, credential environment name, runtime build ID and binding-manifest revision before transmission admission; exact replays require the same provenance. The application uses the versioned snapshot wrapper, while the production v2 runtime target resolver remains deliberately empty. Current local evidence: 1,841 unit tests passed with one existing skip; typecheck and lint passed; the CFG-001 clean fresh-reset authority gate passed 41 tests; and the full serial real-DB suite passed 436 tests across 38 files. The full DB run took 640 seconds on Windows, so its child timeout is now 15 minutes, still below the workflow's 20-minute job limit. Exact-head GitHub and final Relay convergence remain required before handoff.
